# 文件上传安全

服务端清理文件名，拒绝控制字符、双扩展名、绝对路径、盘符、反斜杠、NUL 和 `..`。文件名不用于物理路径。上传同时检查扩展名、分类允许类型与 PDF/JPEG/PNG/WEBP 签名，写入过程计算 SHA-256。

下载和预览始终经过会话、附件权限及学员组织范围校验，不公开静态目录。响应设置 `nosniff`；敏感资料使用 `private, no-store`。当前未接入病毒扫描，预留 QUARANTINED 状态，未知或不匹配内容会拒绝。
