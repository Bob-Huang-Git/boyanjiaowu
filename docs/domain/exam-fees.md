# 考试费用认定

`ExamFeeAssessment` 是考试业务产生的应收依据。每次初考或补考尝试各有独立记录，包含科目、尝试、费用策略版本、整数分金额、责任方和政策描述快照。

初考与补考分别使用 `INITIAL_EXAM` 和 `RESIT`。补考默认由学员承担；金额必须能从批次科目配置或报名绑定的费用策略确定，否则阻止报名。确认、减免和取消均为有审计的状态动作。

本表没有 `paid` 字段，也不代表已经收款。Sprint 5 财务模块将读取已确认费用依据，建立正式应收、收款、退款和代缴流水。
